[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Vite 8 requires ^20.19 || >=22.12; pinned so a change to Netlify's
  # default image never breaks the build.
  NODE_VERSION = "22"

